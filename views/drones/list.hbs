<h2>Available drones</h2>

{{#each drone}}
    <ul>
        <li> Name: {{this.name}}</li>
        <li>Propellers: {{this.propellers}} </li>
        <li>Max. Speed: {{this.maxSpeed}} </li>
    </ul>
    <a href="/drones/{{_id}}/edit">Edit</a>
    <form method="POST" action="/drones/{{_id}}/delete"><button>Delete</button></form>
{{/each}}